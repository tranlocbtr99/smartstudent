services:
  - type: web
    name: exam-ai-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: ALLOWED_ORIGINS
        value: https://smartstudent-omega.vercel.app,http://localhost:5173
